stages:          # List of stages for jobs, and their order of execution
  - build
  - test
  - deploy

build-job:       # This job runs in the build stage, which runs first.
  stage: build
  script:
    - echo "Compiling the code..."
    - echo "Compile complete."
    - echo $CI_PROJECT_DIR
    - cd $CI_PROJECT_DIR/helpers/starter && ./cloud_setup.sh
  tags:
    - "cloud"
  when: manual

unit-test-job:   # This job runs in the test stage.
  stage: test    # It only starts when the job in the build stage completes successfully.
  script:
    - echo "Running unit tests... This will take about 60 seconds."
    - sleep 60
    - echo "Code coverage is 90%"
  tags:
    - "shell-cloud"
  when: manual

pull-from-registry-job:  
  stage: test
  script:
    - docker login -u $GITLAB_USER -p $PERSONAL_ACCESS_TOKEN $CI_REGISTRY
    - docker push registry.gitlab.com/massimo-gollo/mora-deployment:cloud-vps
  tags:
    - "cloud"
  when: manual

deploy-job:      # This job runs in the deploy stage.
  stage: deploy  # It only runs when *both* jobs in the test stage complete successfully.
  script:
    - echo "Deploying application..."
    - echo "Application successfully deployed."
  tags:
    - "shell-cloud"
  when: manual
